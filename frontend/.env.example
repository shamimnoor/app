# Frontend env (also Vercel project env vars).
# Copy to .env and fill in real values. .env is gitignored.

REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJI...your-anon-key...
REACT_APP_FOUNDER_EMAIL=abdullahmuhammadshamimreza@gmail.com
REACT_APP_N8N_EMBED_URL=https://your-n8n.example.com

# Server-side env (used by /api/copilot Vercel Edge Function only — NOT exposed to the browser).
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-5.2

# Leave BLANK in Vercel; the frontend will call /api/copilot on the same origin.
# In Emergent preview set this to the public preview URL of the FastAPI shim.
REACT_APP_BACKEND_URL=
